apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Reservation-service.fullname" . }}-test-connection"
  labels:
    {{- include "Reservation-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Reservation-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
