apiVersion: v1
kind: Service
metadata:
  name: {{ include "Admin-service.fullname" . }}
  labels:
    {{- include "Admin-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Admin-service.selectorLabels" . | nindent 4 }}
