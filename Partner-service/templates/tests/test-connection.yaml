apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Partner-service.fullname" . }}-test-connection"
  labels:
    {{- include "Partner-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Partner-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
