apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Search-service.fullname" . }}-test-connection"
  labels:
    {{- include "Search-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Search-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
